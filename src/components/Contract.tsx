import React from 'react';

interface IProps {
    name: string,
    email: string
}

const Contract = (props: IProps) => {
    return (
        <div>
            
        </div>
    );
};

export default Contract;